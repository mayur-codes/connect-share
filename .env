# In dev, the Vite proxy handles /api and /ws — do NOT set absolute URLs here,
# or the browser will bypass the proxy and hit api.odnix.org cross-origin.
# VITE_REST_BASE_URL=https://api.odnix.org
# VITE_WS_BASE_URL=wss://api.odnix.org
# VITE_MEDIA_BASE_URL=https://api.odnix.org
